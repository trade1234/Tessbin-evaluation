{
  "name": "tessbin-evaluation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "node --watch backend/src/server.js",
    "start:backend": "node backend/src/server.js",
    "build": "npm --prefix frontend run build",
    "test": "node --test backend/test/validation.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^8.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.24.1",
    "nodemailer": "^9.0.3"
  }
}
